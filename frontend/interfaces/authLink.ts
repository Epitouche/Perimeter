/**
 * Represents the OAuth link object with the following properties:
 * @property {string} authentication_url - The URL to authenticate the user
 */
export interface OAuthLink {
  authentication_url: string;
}
