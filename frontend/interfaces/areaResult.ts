/**
 * Represents an area result object with the following properties
 * @property {number} id - The id of the area
 * @property {string} result - The result of the area
 * @property {string} created_at - The date the area
 * @property {string} update_at - The date the area
 */
export interface AreaResult {
  id: number;
  result: string;
  created_at: string;
  update_at: string;
}
