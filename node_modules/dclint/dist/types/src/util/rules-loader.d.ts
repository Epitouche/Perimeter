import type { LintRule } from '../linter/linter.types';
import type { Config } from '../config/config.types';
declare function loadLintRules(config: Config): Promise<LintRule[]>;
export { loadLintRules };
