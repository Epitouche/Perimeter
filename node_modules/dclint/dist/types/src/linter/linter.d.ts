import type { Config } from '../config/config.types';
import type { LintResult } from './linter.types';
declare class DCLinter {
    private readonly config;
    private rules;
    constructor(config?: Config);
    private loadRules;
    private lintContent;
    private fixContent;
    private static validateFile;
    lintFiles(paths: string[], doRecursiveSearch: boolean): Promise<LintResult[]>;
    fixFiles(paths: string[], doRecursiveSearch: boolean, dryRun?: boolean): Promise<void>;
    formatResults(lintResults: LintResult[], formatterName: string): Promise<string>;
}
export { DCLinter };
