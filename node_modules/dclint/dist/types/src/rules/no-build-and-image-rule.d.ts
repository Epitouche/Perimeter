import type { LintContext, LintMessage, LintMessageType, LintRule, LintRuleCategory, LintRuleSeverity, RuleMeta } from '../linter/linter.types';
interface NoBuildAndImageRuleOptions {
    checkPullPolicy?: boolean;
}
export default class NoBuildAndImageRule implements LintRule {
    name: string;
    type: LintMessageType;
    category: LintRuleCategory;
    severity: LintRuleSeverity;
    meta: RuleMeta;
    fixable: boolean;
    private readonly checkPullPolicy;
    constructor(options?: NoBuildAndImageRuleOptions);
    getMessage({ serviceName }: {
        serviceName: string;
    }): string;
    check(context: LintContext): LintMessage[];
}
export {};
