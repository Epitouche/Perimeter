import type { LintRule, LintMessage, LintRuleCategory, RuleMeta, LintRuleSeverity, LintMessageType, LintContext } from '../linter/linter.types';
export default class RequireProjectNameFieldRule implements LintRule {
    name: string;
    type: LintMessageType;
    category: LintRuleCategory;
    severity: LintRuleSeverity;
    meta: RuleMeta;
    fixable: boolean;
    getMessage(): string;
    check(context: LintContext): LintMessage[];
}
