import type { LintContext, LintMessage, LintMessageType, LintRule, LintRuleCategory, LintRuleSeverity, RuleMeta } from '../linter/linter.types';
interface RequireQuotesInPortsRuleOptions {
    quoteType: 'single' | 'double';
}
export default class RequireQuotesInPortsRule implements LintRule {
    name: string;
    type: LintMessageType;
    category: LintRuleCategory;
    severity: LintRuleSeverity;
    meta: RuleMeta;
    fixable: boolean;
    getMessage(): string;
    private readonly quoteType;
    private readonly portsSections;
    constructor(options?: RequireQuotesInPortsRuleOptions);
    private getQuoteType;
    private static extractValues;
    check(context: LintContext): LintMessage[];
    fix(content: string): string;
}
export {};
