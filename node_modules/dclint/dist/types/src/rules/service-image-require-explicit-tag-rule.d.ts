import type { LintContext, LintMessage, LintMessageType, LintRule, LintRuleCategory, LintRuleSeverity, RuleMeta } from '../linter/linter.types';
interface ServiceImageRequireExplicitTagRuleOptions {
    prohibitedTags?: string[];
}
export default class ServiceImageRequireExplicitTagRule implements LintRule {
    name: string;
    type: LintMessageType;
    category: LintRuleCategory;
    severity: LintRuleSeverity;
    meta: RuleMeta;
    fixable: boolean;
    getMessage({ serviceName, image }: {
        serviceName: string;
        image: string;
    }): string;
    private readonly prohibitedTags;
    constructor(options?: ServiceImageRequireExplicitTagRuleOptions);
    private isImageTagExplicit;
    check(context: LintContext): LintMessage[];
}
export {};
